# 发布审计：2026-08-28

## 结论：可附带限制分享，不可作为完成版加速论文成绩

本次打包采用研究记录的 Fact/Inference/Hypothesis/Risk/Experiment 分类，并校验指标计算、图表语义和原始记录来源。用户指定交付物是 GitHub 仓库，因此采用 Markdown 文档、JSON 和静态图，不额外创建报告站点。

## 修正与重要限制

| 级别 | 原问题 | 本次处理 |
|---|---|---|
| 高 | 把 DiT-like 代理估算当成真实模型加速 | README/结果文档明确“未测端到端”；所有估算列带 proxy |
| 高 | 旧 Oracle 实际逐像素 min，却被解释成每块选 Copy/Warp | 保留旧字段用于追溯，新增真正逐块候选 Oracle；重算结果 |
| 高 | 大代理未测 <12.5% active Q，却向原点线性外推 | 同时给出 legacy 与 floor 敏感性，拒绝把 7.22× 当成实测 |
| 高 | PnP 和路由标签都使用未来目标 RGB | 定义为离线、非因果评估；不计作在线模块 |
| 高 | 90→93 的显著 Warp 优势可能混入展示视频转场 | 不再用它证明强转向收益；对 target≥90 的 6 对做排除敏感性 |
| 中 | “safe tile”可能被误读为感知质量保证 | 中文改称 RGB 阈值通过率；保留原始字段名供审计 |
| 中 | gap8 质量套用到 K8 周期所有非 anchor 帧 | 在公式旁注明未覆盖 age1…7，且均值代入非线性延迟只是假设 |
| 中 | 16×16 RGB 块比例直接当作 active latent token 比例 | 明确映射未经验证、边缘 tile 等权，缺少 VAE/时空压缩因素 |
| 中 | 只有两个短裁剪、动态与 HUD 未分离 | 不作跨游戏泛化或交互正确性结论 |
| 中 | 旧 `pair_count` 只有 short 26 对 | 发布汇总正确记录全部 59 有效、1 失败；保留历史 JSON 不改数值 |
| 低 | large 代理图误写 8-block | 新图显示 12-block，原始测量不变 |
| 低 | 多次提取 crop 覆盖 metadata，缺失帧被静默略过 | 新版提取合并记录并验证源一致；评估显式列出 skipped |

## 数值核查

- 从原始样本重新核验几何、CUDA、CPU、大小代理的中位数，允许 0.002 ms 的已记录舍入差。
- 59 个 `(crop, source, target)` 唯一，失败案例单列，没有把失败遗留目录加入成功样本。
- 26 份发布 JSON 数值不变；个人路径相对化，原始和发布 SHA-256 记录在 provenance。
- 真正逐块 Oracle 从已有误差数组/RGB/mask 重新计算，检查和 Copy/Warp 的包含关系。
- 排除转场风险后，Copy 平均 PSNR 仍高于 Warp；并未筛选出一个支持预设结论的主表。
- 图表由同一份审核数据重建，比例、单位和样本量标注清楚。结果表是精确查询，图是趋势/比较辅助。

## 图表映射与 QA

| 图 | 问题/类型 | 数据/粒度 | 解释边界 |
|---|---|---|---|
| warp_runtime | 同任务 CPU/CUDA 耗时；水平柱 | GTA 3 个组件路径中位数 | 线性零起点，不是系统加速 |
| active_query_scaling | active Q 与延迟；数值轴折线、大小模型分面 | 大代理5点，小代理6点 | 不是时间序列；随机权重、驻留 KV |
| copy_warp_quality | Copy/Warp 质量与敏感性；分组柱 | 两个裁剪；all13 vs pre90的12对 | 未来目标辅助位姿，样本相关 |
| tile_oracle_audit | 不同 Oracle 的宽松程度；分组柱 | gap8，每裁剪6对 | MAE test，不是感知安全 |

配色采用蓝色+灰色，Oracle 比较增加金色/粉色；使用标签与旧 Oracle 斜线区分。少量离散记录不伪装成高密度统计趋势。游戏示例为原始研究截图，未美化/生成补图；转场示例仅用于解释限制。

## 未完成的验证

没有真实 DiT 训练/推理集成、没有 L40/H100 实测、没有输入到屏幕延迟、没有质量路由器与修复网络、没有因果玩家控制验证、没有高质量多游戏测试集。当前录制视频和未来目标 PnP 不能替代这些证据。

## 本次发布检查

2026-08-28：`python -m unittest discover -s tests -v` 的 20 项测试全部通过，包括可用 RTX 4060 上的 CUDA identity 与 CPU 对照；`compileall`、结果重建、中位数校验、来源文件 SHA-256、重复帧对检查、相对文档链接和常见敏感文件扫描通过。原始 GTA CPU/CUDA 数组再次核对 mask IoU=1.0、最大深度差=0。这些是代码和记录完整性检查，不是新一轮性能 benchmark。
